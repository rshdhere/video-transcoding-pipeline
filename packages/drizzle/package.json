{
  "name": "@vtp/drizzle",
  "type": "module",
  "private": true,
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "db:generate": "bun --env-file=../../apps/server/.env x drizzle-kit generate",
    "db:migrate": "bun src/migrate.ts",
    "db:migrate:kit": "bun --env-file=../../apps/server/.env x drizzle-kit migrate",
    "db:push": "bun --env-file=../../apps/server/.env x drizzle-kit push",
    "db:studio": "bun --env-file=../../apps/server/.env x drizzle-kit studio",
    "check-types": "tsc --noEmit"
  },
  "dependencies": {
    "drizzle-orm": "^0.44.5",
    "postgres": "^3.4.7"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/pg": "^8.20.0",
    "@vtp/typescript-config": "workspace:*",
    "drizzle-kit": "^0.31.4",
    "pg": "^8.21.0",
    "typescript": "^5"
  }
}